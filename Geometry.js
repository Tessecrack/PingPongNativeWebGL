class Geometry {
    constructor(x, y, width, height, vertexCount, points) {
        this.x = x
        this.y = y

        this.width = width
        this.height = height
        this.vertexCount = vertexCount
        this.points = points
    }
}

export default Geometry